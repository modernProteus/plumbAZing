# Copy to .env (gitignored) and fill in real values for local bake testing.
# Run scripts with Node's built-in env-file support, e.g.:
#   node --env-file=.env build/bake-content.mjs
#   node --env-file=.env build/bake-reviews.mjs
#   node --env-file=.env build/bake-carousel.mjs

APPS_SCRIPT_URL=
GOOGLE_PLACES_API_KEY=
PLUMBAZING_PLACE_ID=
